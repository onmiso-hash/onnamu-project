# TASK 2026-08-18 — Chronicle AI Studio 수위(chatLevel) 소유권 정리

## 신고

"설정에서 수위를 바꿔도 대화에 들어가면 옛 수위로 되돌아간다."

## 처음 한 일 — 잘못 짚은 수정

웹에서 받아온 수정안을 그대로 반영해 배포했다(커밋 `eaabfcb`).
`savePersona()`에서 인물 설정을 저장할 때 `savedSession.chatLevel`도 함께 갈아끼우는 내용이었다.

**효과가 없었다.** 그 코드가 손대는 곳은 브라우저 안 옛 저장소(localStorage)의
`savedSession`이고, 지금 대화는 전부 서버 파일(`data/users/<u>/conversations/<id>/meta.json`)에서
온다. 증상이 나는 길을 아예 지나가지 않는 수정이었다.

증상만 보고 건네받은 수정을 반영했을 뿐, **원인을 끝까지 따라가지 않은 것이 잘못**이다.

## 실제 원인 — 수위의 주인이 셋이었다

같은 값이 세 곳에 살고 서로를 덮고 있었다.

| 자리 | 값 | 누가 이기나 |
|---|---|---|
| 인물 설정 (`characters/<id>.json`의 `level`) | 사용자가 설정창에서 바꾸는 값 | 대화를 열 때 먼저 얹힌다 |
| 대화 (`conversations/<id>/meta.json`의 `chatLevel`) | 대화를 만들 때 박힌 값 | **그 위에 덮어써서 최종 승자** |
| 브라우저 옛 세션 (`savedSession.chatLevel`) | 서버 이사 전 데이터 | 설정창 표시를 덮고 있었다 |

`applyConversationContext()`가 `applyCharacterOfConversation()`으로 인물 수위를 얹은 **직후**
`conv.chatLevel`로 다시 덮는다(app.js). 그래서 인물 설정을 무엇으로 바꿔도 대화에 박힌 값이 이긴다.

또한 `loadSelectedPersona()`는 설정창 표시값을 `savedSession.chatLevel` → `data.level` 순으로 골라,
**설정창 자체가 옛 대화 값을 보여주고** 있었다.

### 미니PC 실측 (2026-08-18)

| 대상 | 저장된 수위 |
|---|---|
| 인물 "유나" (`b202f5da`)의 기본값 | `normal` |
| 유나의 대화 1 | `adult-19` |
| 유나의 대화 2 | `adult-19` |
| 박민정(`50b7c18a`)의 대화 2개 | 하나는 `normal`, 하나는 `adult-19` |

마지막 줄이 결정적이다 — **같은 인물의 대화끼리 이미 값이 갈려 있다.**
"인물 설정으로 통일한다"를 택하면 실데이터가 덮인다.

## 사용자 결정 — 역할을 가른다

- 설정창의 수위 = **그 인물의 기본값**
- 새 대화 = 기본값을 물려받는다
- 대화 중에는 **그 대화의 수위만** 바꾼다
- 대화를 다시 열면 그 대화에 저장된 수위

## 구현 (커밋 `2e45dbc`)

| 파일 | 변경 |
|---|---|
| `index.html` | 사이드바 "내 대화" 칸에 `btn-toggle-chat-level` 신설 |
| `app.js` | `toggleConversationChatLevel()` — 누르면 전환 후 `PATCH /api/conversations/:id {chatLevel}`. 실패 시 원상복구 + 경고 |
| `app.js` | `defaultChatLevelOfCurrentCharacter()` — 서버 인물 → 브라우저 인물 → 현재값 순, 일반 계정은 19금 강등 |
| `app.js` | `updateChatLevelMenu()` — 라벨 갱신, 일반 계정·소설 모드에서는 숨김 |
| `app.js` | `startNewConversation()` — 새 대화는 기본 수위로 시작 |
| `app.js` | `loadSelectedPersona()` — 설정창은 `data.level`만 본다 |
| `app.js` | `savePersona()` — `eaabfcb`의 `savedSession` 덮어쓰기 **되돌림** |

서버는 손대지 않았다. `PATCH`가 이미 `chatLevel`을 받고(`META_PATCH_KEYS`),
일반 계정의 19금 변경을 거절한다(`blocksAdultLevel`).

## 검사

- 화면 동작 **22건** — app.js를 가짜 DOM 위에서 실제로 실행해 확인
  (기본값 물려받기 4 · 메뉴 표시 5 · 그 대화만 저장 6 · 라벨 추종 1 · 설정창 기본값 1 · 새 대화 2 등)
- 저장 동작 **5건** — `store.js`를 임시 데이터 폴더로 실행.
  유나 대화 두 개 중 하나만 19금으로 바꿔도 나머지가 일반으로 남는 것을 파일에 써서 확인

## 배포

- `eaabfcb` 1차 시도 실패 — 깃허브가 `appleboy/ssh-action` 내려받기를 **429(요청 과다)** 로 3회 거절,
  `Set up job` 단계에서 끝났다. 코드·설정 문제가 아니며 재실행으로 해결.
  로컬에 `gh` CLI가 없어 확인은 GitHub REST API(`/actions/runs`, `/check-runs/:id/annotations`)로 했다.
- 최종 확인은 **컨테이너 안 파일이 아니라 HTTP로 실제 배포된 `app.js`를 받아** 대조.

## 남은 것

유나의 대화 2개는 여전히 `adult-19`다. 새 규칙에서는 그것이 그 대화의 수위이므로
자동 변환하지 않았다 — 화면의 메뉴로 대화별로 바꾼다.

---

# 계정관리 1묶음 — 계정 표 신설·이관·해시 로그인 (2026-08-18)

설계: `history/DESIGN_ACCOUNTS_20260818.md` (4묶음 중 **1묶음**)

## 왜

계정 2개가 `portal/users.json`에 **평문 비밀번호**로 있었고, 이 저장소는 공개다.

## 착수 전 재실측 (설계서가 지시한 3자리)

| 자리 | 결과 |
|---|---|
| `portal/app.py` `load_users_from_file` | 그대로 파일을 읽고 있었다 |
| `gallery/app.py` 업로드 폴더 고르기 2곳 (289-294 / 381-385) | 그대로 |
| `studio/server.js:26` `adminOnly` 빗장 | 그대로 |

werkzeug 해시는 **포털 컨테이너 안에서 실행해** 가능한 것을 확인(새 의존성 0개).

## 무엇을 했나 (`portal/app.py` 한 파일)

- `init_db()`에 `accounts` 표 신설 — 설계서의 9개 칸 그대로
- `migrate_users_once()` — `users.json`을 1회 읽어 해시해 옮긴다
- 로그인이 표를 본다(`get_account` + `check_password_hash`), `last_login_at` 기록
- 잠긴 계정은 맞는 비밀번호로도 거절

### 이관 시 판단 2가지

- **19금 열람은 관리자만 켠다** — 지금 스튜디오가 관리자 전용이라 그것이 현 상태와 같다
- **올리기는 `public` 아닌 폴더를 가진 계정만 켠다** — 지금 올릴 수 있던 그대로

### 이관 여부 판정

`users.json`이 컨테이너에 **읽기 전용(`:ro`)으로 붙어 있어** 원본에 이관 표시를 쓸 수 없다.
그래서 **"표가 비어 있는가"** 를 판정으로 삼았다. 두 번 돌려도 덮어쓰지 않는 것을 검사로 확인.

## 검사

- 로컬 **27건** — 포털을 임시 폴더에 띄워 실제 기동. 이관 10 · 재실행 2 · 로그인 9 ·
  출입증 3 · 시각 1 · 잠금 2 · 파일 삭제 후 로그인 1
- 배포 후 미니PC 실계정 — **두 계정 모두 쓰던 비밀번호가 그대로 통과**(해시 대조 + 실제 HTTP 로그인 302),
  틀린 비밀번호·없는 아이디는 200(거절)

## 남은 것

2묶음(관리 화면) · 3묶음(권한 즉시 반영·갤러리 올리기) · 4묶음(스튜디오 빗장 해제).
**3 없이 4를 하면 19금 허용을 꺼도 30일간 안 꺼진다.**
